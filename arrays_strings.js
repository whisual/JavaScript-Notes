// ------------------------------------------------ ARRAY METHODS ------------------------------------------------------//


// let arr = [1, 3, 5, 7, 9, 2, 4, 6, 8,]

// console.log(arr.map((num) => { return num * 10 }))   it changes all the elements of an array in a desired way

// console.log(arr.filter((num) => { return num % 2 == 0 }))  it removes the desired elements from an array

// console.log(arr.reduce((acc, num) => { return acc + num }, 0))  it adds all the elements of an array

// arr.push("Puneet") it adds an element at the ending of an array
// console.log(arr);

// arr.pop()   it removes an element at the ending of an array
// console.log(arr);

// arr.shift()   it removes an element from the starting of an array
// console.log(arr);

// arr.unshift(1)   it adds the element in the starting of an array
// console.log(arr);

// let arr2 = arr.join()   it converts array into a string
// console.log(arr2);

// console.log(arr.length); it shows the length of an array

// let arr3 = arr.slice(1, 5)   it slices the array and returns the elements from given indexes
// console.log(arr3);

// let arr4 = arr.splice(1, 5)   it slices the array returns the elements from given indexes and deletes the elements from original array
// console.log(arr4);    here it returned from the given indexes
// console.log(arr);     here it deleted them from original array


//-------------------------------------------- STRING METHODS ------------------------------------------------------------//


// let str = "   VishalSharma   "

// console.log(str.length)    it shows the length of a String

// console.log(str.toLowerCase())  it convetrs the string to lowercase
// console.log(str.toUpperCase());  it converts the string to uppercase

// console.log(str.replace("i", "x"))   it changes the specific element in a string

// console.log(str.charAt(0));    it shows the character at asked position


// let str2 = "SmartBoy"    it adds two strings i a paragraph
// console.log(`My name is ${str} and i am a ${str2}`);

// let str3 = str.slice(0, 6)     it slices the string from given indexes
// console.log(str3);

// console.log(str);
// console.log(str.trim());   it trims the unwanted spaces from starting and ending


